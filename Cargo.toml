[package]
name = "netcdf-merge-server"
version = "0.1.0"
edition = "2024"

[dependencies]
rocket = "0.5"
netcdf-reader = { version = "0.4.0", features = ["netcdf4"] }
tokio = { version = "1", features = ["sync"] }
